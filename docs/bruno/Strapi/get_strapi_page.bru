meta {
  name: Get Strapi Page
  type: http
  seq: 12
}

get {
  url: {{strapiHost}}/api/pages?filters[url][$eq]={{path}}
  body: none
  auth: bearer
}

params:query {
  filters[url][$eq]: {{path}}
}

auth:bearer {
  token: {{strapiKey}}
}

vars:pre-request {
  path: /a-propos/
}
